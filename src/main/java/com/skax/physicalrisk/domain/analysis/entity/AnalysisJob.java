package com.skax.physicalrisk.domain.analysis.entity;

public class AnalysisJob {
    // AnalysisJob 엔티티: id, siteId, jobId, status, progress, currentNode,
    // startedAt, completedAt, estimatedCompletionTime, error
}
